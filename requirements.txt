playwright>=1.45.0
curl_cffi>=0.7.1
pydantic>=2.7.0
duckdb>=1.0.0
streamlit>=1.38.0
pandas>=2.2.0
# Local HTTPS-intercepting proxy used by hybrid_scraper.mobile_session to
# capture the Coles mobile app's device-attestation token off a real,
# already-logged-in Android Emulator session (see that module's docstring).
mitmproxy>=12.0.0
# Console progress bars/tables for long multi-store scrape runs (demo_scrape.py).
rich>=13.0.0
